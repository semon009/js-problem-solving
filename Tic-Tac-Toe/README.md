# Tic-Tac-Toe Result Grid

## Problem Description
Write a program that displays the final 3x3 result grid of a Tic-Tac-Toe game.
* The letters `X` and `O` must be uppercase.
* Note that `O` is the letter O, not the number zero `0`.

### Expected Output
```text
X X O
O X O
O O X
