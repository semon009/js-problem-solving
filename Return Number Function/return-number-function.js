/**
 * Problem: Return Number Function
 * 
 * Description:
 * Create the 'returnNumber' function that takes a number as a parameter and returns it.
 * 
 * Example:
 * returnNumber(5) -> returns 5
 * returnNumber(42) -> returns 42
 */

// Solution: Function that accepts a number parameter and returns it
function returnNumber(x) {
  return x;
}

// Execute and log result
console.log(returnNumber(5));
